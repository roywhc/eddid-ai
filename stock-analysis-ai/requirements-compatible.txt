# Compatible version set for Python 3.13 (with pre-built wheels)
# Based on Perplexity research for avoiding Rust build requirements

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.3.0

# LLM & RAG (LangChain v1)
# Using compatible versions that work together
langchain==1.0.0
langchain-core>=1.0.0,<2.0.0
# Pin to 0.3.0 to avoid dependency resolution issues
langchain-community==0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0

# Vector Store & Embeddings
# Use chromadb 0.6.0+ which removes tokenizers upper bound
# This allows using newer tokenizers with pre-built wheels
chromadb>=0.6.0
sentence-transformers==3.0.0

# External Knowledge Integration
openai>=1.40.0,<3.0.0
instructor>=1.4.0

# Database & Storage
sqlalchemy>=2.0.0,<2.1.0
# Note: psycopg2-binary is optional and only needed for pgvector
# Default vector store is ChromaDB which doesn't require PostgreSQL
# To install PostgreSQL support: pip install -r requirements-optional.txt

# Async & Concurrency
# Use newer aiohttp that has better wheel support
aiohttp>=3.10.0
asyncio-contextmanager>=1.0.1

# Utilities
python-dotenv==1.0.1
requests==2.32.0
httpx==0.27.0
tenacity==9.0.0

# Logging & Observability
python-json-logger==2.0.7
structlog==24.2.0

# Testing
pytest==8.2.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0

# Development
black==24.10.0
flake8==7.1.0
mypy==1.13.0


