# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.3.0

# LLM & RAG (LangChain v1)
langchain==1.0.0
langchain-core>=1.0.0,<2.0.0
# Pin to 0.3.0 to avoid long dependency resolution times
langchain-community==0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0

# Vector Store & Embeddings
# Use chromadb>=0.6.0 to avoid tokenizers version conflict
# chromadb 0.6.0+ removes upper bound on tokenizers, allowing newer versions with pre-built wheels
chromadb>=0.6.0
sentence-transformers==3.0.0

# External Knowledge Integration
# Note: openai 1.40.0 requires jiter that needs Rust on Python 3.13
# Using flexible version - will use pre-built wheels when available
# If you have openai already installed, it will use that version
openai>=1.40.0,<3.0.0
# instructor: Using >=1.4.0 to get jiter>=0.6.1 which has pre-built wheels
instructor>=1.4.0

# Database & Storage
sqlalchemy>=2.0.0,<2.1.0
# Note: psycopg2-binary is optional and only needed for pgvector
# Default vector store is ChromaDB which doesn't require PostgreSQL
# To install PostgreSQL support: pip install -r requirements-optional.txt

# Async & Concurrency
# Use flexible version to allow newer versions with better wheel support
aiohttp>=3.10.0
asyncio-contextmanager>=1.0.1

# Utilities
python-dotenv==1.0.1
requests==2.32.0
httpx==0.27.0
tenacity==9.0.0

# Logging & Observability
python-json-logger==2.0.7
structlog==24.2.0

# Testing
pytest==8.2.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0

# Development
black==24.10.0
flake8==7.1.0
mypy==1.13.0
